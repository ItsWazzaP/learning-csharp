public class Program {
    public static void Main(string[] args) {
        // Type your code below
        bool b1 = true;
        bool b2 = false;
        bool b3 = b1 || b2; // || = OR, && = AND
        
        // Don't change the line below
        System.Console.WriteLine("b3 = " + b3);
    }
}